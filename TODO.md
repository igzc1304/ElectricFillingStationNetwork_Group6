# TODO

- Seperate Step files for each feature file
- Data for tests only in Given, not in step file setup
- Clean up name inconsistencies, i.e. Charging Point, Charging station, ...
- Min 2 datatable in feature files
- CRUD operations for the classes
- Billing, only top up, no invoice
- Missing Data in invoice
- Check all attributes from the domain model
- Convert charging point status to an Enum
- Charging point status has to be checked before a new charging can begin
- Network status is missing (Locations with prices, charging stations and there status)
- More scenarios(other teams have around 30), many for CRUD
- One Error and one edge case per feature file
- Prepare presentation, checklist on moodle
- User Story map has to match code(missing features should be send to the Backlog)
